# Dragon docs audit — final

**63 findings** · 12 CRITICAL · 27 HIGH · 20 MEDIUM · 4 LOW
**Date:** 2026-08-12 · Three review passes (audit → verify → verify-the-verifier), consolidated.

**This file:** `~/git/dragon-kit/DOC-AUDIT-2026-08-12.md` — **untracked scratch, not committed, not
on any branch.** `git status` will show it as untracked in the main clone. Delete it when the work
is split out.

Every path below is written in full from `~`. Line numbers in `~/git/dragon-kit/**` are as of
**`origin/main` = `63d7f1e`**; that clone may be behind, so `git -C ~/git/dragon-kit fetch` first.
Numbers moved once already during this audit (PR #67).

---

## Ground truth (verified 2026-08-12, all `origin/main`)

```
dragon-kit          63d7f1e   newest tag v4.0.0   §R0–§R15 live
clipmenu-2          378ced4   pin 4.0.0    exceptions: []
ice-2               eda8cda   pin 4.0.0    (no exceptions key at all)
spectacle-2         a2a431c   pin 4.0.0    exceptions: []
yahoo-keykey-2      c3f2d6e   DRAGONKIT_TAG=v4.0.0   exceptions: []
dragon-sample-app   822cdd6   pin 4.0.0    exceptions: [R15]   <- the only one anywhere
www.dragonapp.com   7da0f13   docs/dragon-sample-app/ holds ONLY licenses/index.html; 0 hub cards
release CI          all five apps on dragon-release-ci@v6
PRs                 keykey#71 MERGED · ice-2#83 CLOSED · ice-2#56 MERGED
`release` skill     does not exist
dragon-kit Example/ untracked
```

## Severity

| | Meaning |
|---|---|
| **CRITICAL** | Following the doc produces a CI failure or a wrong shipped artifact |
| **HIGH** | Contradicts a live rule, or asserts a false fact about one |
| **MEDIUM** | Stale fact that misleads but breaks nothing |
| **LOW** | Cosmetic |

## Three root causes

1. **§R11's rewrite landed only in §R11.** PR #64 established that four long-listed "sanctioned
   exceptions" were never declared in any app and never needed to be. Nine other places still
   assert the old story — including two agent-facing prompts and, now, **ice-2's own source code**
   (#23). §R11 predicted this: *"the next app copies the shape."*
2. **`Example/` was deleted but is still cited as the reference implementation** — three places.
3. **The checker's guarantees are weaker than the spec claims.** Ten rules advertise enforcement
   they do not have, and the test suite cannot detect it (#13, #14).

---

# CRITICAL

### 1 — liquid-glass SKILL body prescribes a §R4 violation
`~/.claude/skills/liquid-glass-macos/SKILL.md:142-152` ·
`~/.claude/skills/liquid-glass-macos/references/swiftui-recipes.md:84-110`

Says *"use inset-grouped forms (`Form { Section { … } }`)"* and gives a copy-paste recipe. Apps must
use `DragonForm`/`DragonSection`. Running the supplied fragment through the checker yields
`R4 … hand-rolled grouped Form`.

**Worst in the audit:** this is the always-loaded SKILL body, so it reaches every agent that
triggers the skill — not just one that opens a reference file.

### 2 — liquid-glass SKILL body prescribes a free-form `AboutView` and stub URLs
`~/.claude/skills/liquid-glass-macos/SKILL.md:161-171` ·
`~/.claude/skills/liquid-glass-macos/references/swiftui-recipes.md:129-130`

*"Keep one shared `AboutView` differing only by icon/name/links"*, with examples `/clipmenu` and
`/keykey/`. DragonKit 4.0.0 owns the About layout in fixed slots; those two paths are the redirect
stubs §R15 rejects. Recreates removed architecture **and** fails §R15.

### 3 — ClipMenu's About Website row set to the redirect stub
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:44`
`dragonapp.com/clipmenu` — path `clipmenu` ≠ repo `clipmenu-2`. Fails §R15.

### 4 — dual-holder copyright prescribed verbatim
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:46`
`© 2008–2014 Naotaka Morimoto · © 2026 Teddy Chan` — fails §R14 (two `©` on one line). The exact
string 4.0.0 removed.

### 5 — `SPUStandardUpdaterController` prescribed
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:48` — fails §R6; apps use
`DragonUpdater`.

### 6 — free-form About layout prescribed
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:42-46` — `AboutContent` has fixed
slots since 3.0.0; **cannot compile against 4.0.0**.

### 7 — Ice told to keep the code §R4/§R6 forbid
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:166,180,183` — instructs keeping
`IceForm`/`IceSection` and direct `SPUStandardUpdaterController`. Current ice-2 uses
`DragonForm`/`DragonSection` and `DragonUpdater`.
*Correction: the claim that `UpdatesManager` is stale is wrong — it is still live on ice-2 `origin/main`, now wrapping `DragonUpdater`. The §R4/§R6 defect stands.*

### 8 — adoption prompt invents a keykey §R11 exception (Quit)
`~/git/dragon-kit/docs/ADOPT-DRAGONKIT-PROMPT.md:136`
*"(yahoo-keykey-2 additionally records its Quit omission as a sanctioned §R11 exception.)"* False —
keykey's config is `"exceptions": []`; `includeQuit: false` is a kit parameter, i.e. compliance.
CRITICAL because the file is pasted into agents.

### 9 — adoption prompt says an exception is as good as a trait
`~/git/dragon-kit/docs/ADOPT-DRAGONKIT-PROMPT.md:176`
*"…records this as a sanctioned §R11 exception instead; **either is fine**, the trait is tidier."*
False on fact (keykey uses `traits: ["sparkle","no-permissions"]`) and wrong on guidance — §R11
ranks an exception *after* a trait.

### 10 — global instructions name the KeyKey redirect stub as its page
`~/.claude/CLAUDE.md`, repo map — *"KeyKey … marketing page at `www.dragonapp.com/keykey/`"*.
Canonical is `/yahoo-keykey-2/`. **§R15 fails a PR whose About row points at a stub.**

### 11 — §R15 validates neither host *(defect in both checker and kit)*
`~/git/dragon-kit/CONFORMANCE.md:399,421` ·
`~/git/dragon-kit/Scripts/dragon-conformance.py` (`github_repository`, `rule_r15_website_page`) ·
`~/git/dragon-kit/Sources/DragonKit/About/AboutContent.swift:292`

Verified by fixture — this passes §R15 silently:

```swift
websiteURL: URL(string: "https://evil-example.com/fixture-2/")!,
supportURL: URL(string: "https://notgithub.com/teddychan/fixture-2/issues")!,
```

Two holes: the **website host is never checked** (only `urlsplit(website).path`), and
`endswith("github.com")` matches `notgithub.com` / `evilgithub.com`. The second is **inherited from
the kit** — `AboutContent.swift:292` does `url.host?.hasSuffix("github.com")`. Fixing either side
alone re-splits them.

### 12 — lifecycle prompt contradicts the lifecycle spec, and README points at it
`~/git/dragon-kit/docs/IMPLEMENT-MAC-APP-RELEASE-LIFECYCLE-PROMPT.md:27-29` — *"the in-tree app may
remain the integration fixture"*. `~/git/dragon-kit/docs/MAC-APP-RELEASE-LIFECYCLE.md:127-131` says
that allowance expired and tests replace the duplicate app. **`~/git/dragon-kit/README.md:250-251`
still directs agents to the stale prompt.**

---

# HIGH

### 13 — `expect_violation` cannot prove the intended branch fired
`~/git/dragon-kit/Scripts/test_conformance.py:179-188` — asserts only `rule not in out`. Any
violation carrying the same rule ID satisfies it, so a fixture passes while the branch under test is
dead.

**Seven targeted mutations left the whole suite green:** R0 missing-required-key, R1
Check-for-Updates, four missing-pane arms of R5, the `SPUUpdater` arm of R6, the `LaunchAtLogin` arm
of R7, R10 missing-pin, R14 dual-symbol. Whole-*rule* deletion is still caught, so the suite is not
worthless — its branch guarantees are.

**This is the mechanism behind #11:** §R15's tests all passed while the host hole was open.

### 14 — §R0's "MUST anchor on dragon-kit" is unenforced, and the suite codifies the false pass
`~/git/dragon-kit/CONFORMANCE.md:74-79` vs `~/git/dragon-kit/Scripts/test_conformance.py:403-406`

The checker runs whatever regex an app configures without verifying it anchors on DragonKit. An
unanchored pattern read Sparkle `999` instead of the stale DragonKit `1.0` pin and **passed**.

The test at `:403` is `expect_pass("unanchored pattern silently reads Sparkle's version — the
trap", …)` — **the suite asserts the false pass as expected behaviour.** It documents the
most-cited incident in the spec while guaranteeing it stays open.

### 15 — §R13 sees only direct initializer syntax
`~/git/dragon-kit/Scripts/dragon-conformance.py` — `re.finditer(r"\bLanguagePicker\s*\(", body)`.
For an app shipping two locales, both `LanguagePicker.init()` and
`typealias AppPicker = LanguagePicker; AppPicker()` **passed**. And since only `//` is stripped,
`/* LanguagePicker() */` produces a **false violation**.

### 16 — §R14 misses indirect and multiline copyright literals
`~/git/dragon-kit/Scripts/dragon-conformance.py` (`rule_r14_about_copyright`) — claims the copyright
is "kit-assembled" but tests only `copyright: "`, two `©` on one line, and the removed `original:`.
Both `copyright: Self.notice` and a literal on the following line **passed**.

### 17 — §R1/§R2 are line-based substring checks
`~/git/dragon-kit/CONFORMANCE.md:92-100,106-112` vs `rule_r1_r2_menu`. `if "DragonAppMenu" in line`
— a bare `let marker = "DragonAppMenu"` satisfies §R1. The Uninstall check only inspects lines
containing `NSMenuItem(`, so a multi-line `NSMenuItem(\n  title: "Uninstall Test"…)` passes §R2.
*(§R13 solved this class with `strip_literals`; §R1/§R2 never got it.)*

### 18 — §R5 counts a commented-out pane reference
`rule_r5_shared_panes` does `body = "".join(...)` with **no comment stripping**. A file whose only
mentions are `// AboutSettingsPane … PermissionsSettingsPane` passes §R5.

### 19 — omitting a config key silently disables its rule
No `strings` → §R8 never fires (a forbidden `DragonKit.*` key passes). No `paneOrder` → §R9 never
fires (wrong order passes). Both reproduced. §R0 makes a *missing config* a violation for exactly
this reason; the logic was never applied to its keys.

### 20 — §R11's own `reason`/`sanctionedBy` are never validated
`~/git/dragon-kit/Scripts/dragon-conformance.py` (`Config.excuses`) matches on `rule` and `path`
only. `{"rule":"R4","path":"…"}` suppresses the rule and passes, printing `NO REASON GIVEN`. §R11's
core promise is unenforced — **and it now guards a live exception.**

### 21 — §R12 checks for a key, not a commit date
`~/git/dragon-kit/CONFORMANCE.md:289-292` requires writing `git log -1 --format=%cI`; the checker
tests only for the substring `DragonCommitDate`. `~/git/dragon-kit/Scripts/test_conformance.py:434-436`
explicitly expects an **empty** Info.plist value to pass.

### 22 — §R6/§R7 promise a positive rule, implement a blacklist
Prose says updates go *through* `DragonUpdater`, login *through* `LoginItem`, and forbids "no
third-party launch-at-login package". Implementation blacklists Sparkle identifiers, `SMAppService`
and the literal module name `LaunchAtLogin`. Verified: `import LoginServiceKit; let service =
LoginService()` **passes**.

### 23 — ice-2's source cites an §R11 exception it does not declare
`~/git/ice-2/Ice/Settings/SettingsPanes/IceBackupSettingsPane.swift:9-13`

> *"Ice 2 keeps versioned backup files in a user-chosen folder … **(see dragon-kit CONFORMANCE.md
> §R11)**."*

ice-2's `.dragon-conformance.json` has **no `exceptions` key at all**. First instance of the §R11
pattern found *outside* the doc corpus — in shipping app source. The divergence is genuine and
carried by the §R9 Backup slot spelling; only the citation is wrong.

### 24 — §R0 says zero exceptions, §R11 says one
`~/git/dragon-kit/CONFORMANCE.md:83` vs `:226`. §R0's schema comment: *"the real value in all five
apps: empty."* §R0 is the block app authors copy into a new repo.

### 25 — §R4 states a rule the checker deliberately does not implement · **OWNER DECISION**
`~/git/dragon-kit/CONFORMANCE.md:136` — claims it flags *"a type named
`*Form`/`*Section`/`*GroupBox`"*. `LAYOUT_PRIMITIVE` requires a generic `<…: View>` wrapper and
**deliberately excludes `GroupBox`** (no compliant path existed for `IceGroupBox`).

Narrowing the prose is honest but leaves `*GroupBox` unchecked forever; widening the checker
reopens the problem that got it dropped. **Do not pick silently.**

### 26 — §R9 cites an §R11 exception that does not exist
`~/git/dragon-kit/CONFORMANCE.md:188` — clipmenu-2's `SyncBackupPane` sits there *"under §R11"*.
§R11 says it needed no exception; the §R9 slot spelling carries it. clipmenu-2 declares none.

### 27 — global site-structure example names two stub directories
`~/.claude/CLAUDE.md` — *"`docs/<app>/index.html` … (e.g. `clipmenu/`, `keykey/`)"*. Should be
`clipmenu-2/`, `yahoo-keykey-2/`.

### 28 — two skills route release work to a `release` skill that does not exist
`~/.claude/skills/dragon-mac-ops/SKILL.md:104` and
`~/.claude/skills/update-macos-appstore/SKILL.md` (step 1). Dead ends at the moment of shipping.

### 29 — `Example/scripts/run.sh` named as the build-number reference
`~/.claude/skills/dragon-mac-ops/SKILL.md:49-51`. `Example/` is untracked;
`~/git/dragon-kit/CLAUDE.md` says ignore it. Should point at `~/git/dragon-sample-app/scripts/run.sh`.

### 30 — memory index says ice-2 holds the only exception
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/MEMORY.md` — *"§R13 is LIVE and ice-2
holds the only exception."* ice-2 declares none; the only one is dragon-sample-app's R15. **PR #67
has since removed ice-2's reserved §R11 row entirely** — re-read §R11 before wording a fix.

### 31 — memory index says ice-2 PR #83 is on hold
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/MEMORY.md` — **#83 is CLOSED.**

### 32 — a standing instruction protects a closed PR
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/ice-2-localization-decision.md` —
*"**Do not treat it as abandoned.**"* Same closed PR.

### 33 — pins recorded three majors behind
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-adoption-gaps.md` — *"ice-2 and
clipmenu-2 on 1.5.0; yahoo-keykey-2 still on v1.3.0."* All five are on **4.0.0**.

### 34 — claims `Example/` is path-pinned and built by CI
Same file — *"`Example/` tracks kit HEAD by `path: \"..\"` … and CI builds it."* Untracked; not
built; and **§R10 now makes a `{"kind":"path"}` pin a violation.**

### 35 — release-CI version contradiction between two memory files
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/keykey-topology.md` *"Release CI =
`dragon-release-ci@v5`"* vs `…/dragonkit-app-propagation.md` *"all five moved off `@v5`"*. All five
are on **`@v6`**.

### 36 — clipmenu MAS recorded as unresolved `mas-v*` work
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-app-propagation.md` — *"still
uses a SEPARATE `mas-v*` tag… open work."* Resolved 2026-08-11; dispatch-only on the existing
`vX.Y.Z`. Three-way contradiction with `update-macos-appstore` and `~/.claude/CLAUDE.md`.

### 37 — shipped design spec still says "pending", and prescribes the removed copyright
`~/git/dragon-kit/docs/superpowers/specs/2026-08-08-about-pane-canon-design.md` — Status *"approved,
pending implementation plan"*; `:194` *"Three of five apps are wrong today"*; **`:187` prescribes
the dual-holder copyright §R14 rejects.** Shipped as 4.0.0/§R14/§R15. Needs the "Superseded" banner
its sibling spec at `…/specs/2026-07-04-dragon-sample-app-real-app-design.md` already has.

### 38 — an unreproducible verification claim
`~/git/dragon-kit/docs/STARTING-A-NEW-APP.md:704-707` — *"passed `dragon-conformance.py` — R10
excepted, because the scratch package pinned the kit by `path:`"*. Not reproducible: §R10 rejects
path pins outright and `rule_r10_pin` never calls `cfg.excuses("R10", …)`.

### 39 — the v0 plan recreates deleted architecture and a removed API
`~/git/dragon-kit/docs/superpowers/plans/2026-06-30-dragon-kit-v0.md:787-835,959-1197` — recreates
`Example/` and the removed `AboutLink` / free-form `AboutContent` API. Code produced from it cannot
compile against 4.0.0. Needs a superseded banner.
*Correction: `:3`'s "REQUIRED" modifies which sub-skill to use when implementing — it does not say implementation is required. Downgraded from CRITICAL.*

---

# MEDIUM

### 40 — §R3 prose is broader than the rule
`~/git/dragon-kit/CONFORMANCE.md:119` forbids any matching app declaration; the checker deliberately
matches only **top-level** declarations (the `SettingsBackup.BackupError` false positive). Prose must
say "top-level".

### 41 — the intro instructs a pin the same file says does not resolve
`~/git/dragon-kit/CONFORMANCE.md:17` — *"by pinning the workflow at a major tag"* vs `:44-48`
requiring `@main` and stating `@v2` does not resolve.

### 42 — the site does not host the sample app's appcast
`~/git/dragon-kit/CONFORMANCE.md:259` and `:421` — *"licenses/ **and its appcast**"*. Only
`licenses/index.html` is on the site; the appcast is app-owned on raw.githubusercontent. The rest of
§R15's premise is verified correct.

### 43 — clipmenu-2-premium: "no longer built" vs "a Developer ID diagnostic"
`~/.claude/CLAUDE.md` vs `~/.claude/skills/dragon-mac-ops/SKILL.md:12`. A manually-dispatched
Developer ID workflow still exists.

### 44 — global repo map omits three of the five apps
`~/.claude/CLAUDE.md` covers ClipMenu and KeyKey only — **ice-2, spectacle-2 and dragon-sample-app
are absent.**

### 45 — review skill says four downstream apps, its own description says five
`~/git/dragon-kit/.claude/skills/dragonkit-review/SKILL.md:55` vs `:3`.

### 46 — site skill protects two paths that do not exist
`~/.claude/skills/dragonapp-site-optimize/SKILL.md:81` — `docs/appcast.xml` and
`docs/keykey/appcast.xml`. Real ones: `docs/{clipmenu-2,ice-2,yahoo-keykey-2}/appcast.xml`.

### 47 — `includeQuit: false` called a "sanctioned deviation"
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:108,112` — §R11 calls it compliance.

### 48 — stale per-app facts in liquid-glass
`~/.claude/skills/liquid-glass-macos/references/per-app-specs.md:124,168,183` — says keykey and Ice
use site-hosted feeds and Ice has no Uninstall pane. Both feeds are app-owned raw-GitHub URLs; Ice
constructs `UninstallSettingsPane`.

### 49 — memory describes an R13 vacancy note that is correctly gone
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/ice-2-localization-decision.md` — says
R13 is *"held vacant on `main`"* while the same file states #60 MERGED.

### 50 — released sample app recorded as `sample-v1.0.3`
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-adoption-gaps.md` — it is
**v1.4.5** in `teddychan/dragon-sample-app`.

### 51 — ice-2 PR #56 flagged PENDING
Same file — merged.

### 52 — keykey PR #71 recorded as parked
Same file and `~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/keykey-topology.md` —
merged.

### 53 — "sanctioned divergences" list mirrors the retired §R11 table
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-adoption-gaps.md` — the four
cases §R11 now records as compliance via slot spelling, trait or kit parameter.

### 54 — keykey vendors DragonKit at v1.2.1
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/keykey-topology.md` — now `v4.0.0`.
Recipe valid; tag stale.

### 55 — `Example/scripts/run.sh` cited again
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/build-number-git-commit-count.md`.

### 56 — implies the website check has been enforced since v3.0.0
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonapp-site-canonical-app-urls.md` —
*"a wrong link **now fails**"*. Nothing checked it per app until §R15, merged 2026-08-12.

### 57 — cites `sample-app` inside dragon-kit as precedent
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-not-an-attribution.md` — now
lives in `teddychan/dragon-sample-app`. The rule itself is correct.

### 58 — a "deliberate deferral" whose trigger has already fired · **OWNER DECISION**
`~/git/dragon-kit/CLAUDE.md:149-154` and `~/git/dragon-kit/README.md:262-265` — *"Generalizing the
folder shape waits until a second app needs it."* **The second app exists:**
`~/git/ice-2/Ice/Settings/SettingsPanes/IceBackupSettingsPane.swift` ships a user-chosen folder,
versioned files and restore, alongside clipmenu-2's. Needs re-evaluation, not automatic extraction —
what is wrong is the standing "leave this alone" instruction.

### 59 — this audit's ground truth is a snapshot
Heads moved twice during the three passes. Re-fetch before acting on any line number.

---

# LOW

### 60 — liquid-glass covers three of five apps
`~/.claude/skills/liquid-glass-macos/SKILL.md:3` — *"all three apps follow"* is correct within its
declared scope; the gap is coverage of a now-five-app ecosystem.

### 61 — "Five rules" over six bullets
`~/git/dragon-kit/docs/STARTING-A-NEW-APP.md:158`.

### 62 — "four consumers" vs "FIVE consumers" in one file
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragonkit-adoption-gaps.md`.

### 63 — sample app latest tag recorded as v1.4.2
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/dragon-sample-app-release.md` — now
v1.4.5.

---

# Withdrawn — do not action

**W1 · `~/.claude/skills/dragon-mac-ops/SKILL.md:16-17,88-91`** — *"Its release ownership must be
separate from `dragon-kit`"*. Read initially as pending work; it is a **standing invariant with its
rationale**. Confirmed twice.

**W2 · `~/.claude/CLAUDE.md`** — *"KeyKey … (Topology not yet fully mapped — confirm before acting
on it.)"* Flagged as stale because
`~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/keykey-topology.md` exists. But that file
carries obsolete tags, a wrong PR state and an unresolved IMK dispatch question (#35, #52, #54) — a
file existing is not a topology being mapped. **The caveat is correct; keep it.** Revisit only after
the memory is fixed.

---

# Verified correct — do not churn

- **§R15's core premise.** `docs/dragon-sample-app/` on the site holds only `licenses/index.html`;
  zero hub cards. The exception is justified. *(Only the appcast clause is wrong — #42.)*
- **`~/git/dragon-kit/docs/MAC-APP-RELEASE-LIFECYCLE.md`** — fully read three times, zero defects.
- **All five `.dragon-conformance.json`** — every `sources`/`strings`/`pin`/`paneOrder` target
  resolves; all five pass the current checker; `swift test` is 130 tests green.
- **`~/git/dragon-kit/README.md` module descriptions** vs the public API in
  `~/git/dragon-kit/Sources/DragonKit/` — accurate.
- `~/.claude/skills/macos-debug-build/`, `~/.claude/skills/macos-swift-engineering/` — no DragonKit
  claims to go stale.
- Memory files with no findings: `conformance-rule-numbering.md`, `pr-first-release-flow.md`,
  `keykey-debug-ime-stays-installed.md`, `verifying-a-mirrored-appcast.md`,
  `execute-remedies-printed-in-error-text.md`, `dragonapp-site-publish-invariant.md`,
  `dragonapp-site-changelog-automation.md`, `verifying-dragon-app-ui-at-runtime.md`,
  `verifying-builds-across-dragon-repos.md` — all under
  `~/.claude/projects/-Users-teddychan-git-dragon-kit/memory/`.
- *"all four shipping apps"* (`~/git/dragon-kit/CONFORMANCE.md:22`) — historically accurate for the
  pre-2026-08-04 state. Not a bug.
- Hardcoded versions in prose ("DragonKit 3.4.0 added…") — historical facts, not instructions.
- `sample-app/` mentions in `~/git/dragon-kit/CONFORMANCE.md:9` and
  `~/git/dragon-kit/CLAUDE.md:7,29,171` — past-tense narrative.

---

# Execution order

| Batch | Findings | Why this order |
|---|---|---|
| **1. liquid-glass SKILL body** | 1, 2 | Always-loaded; reaches every agent that triggers the skill |
| **2. Test-suite integrity** | 13, 14 | Must precede batch 3 — otherwise every checker fix lands unverified |
| **3. Checker false passes** | 11, 15, 16, 17, 18, 19, 20, 21, 22, 40 | Triads (rule + checker + test). **11 also needs the kit fix** at `~/git/dragon-kit/Sources/DragonKit/About/AboutContent.swift:292` |
| **4. liquid-glass per-app-specs** | 3, 4, 5, 6, 7, 47, 48 | Six CI-failing prescriptions in one file |
| **5. dragon-kit docs** | 8, 9, 12, 37, 38, 39, 41, 42, 61 | 12 also needs the `README.md:250-251` pointer fixed |
| **6. CONFORMANCE.md** | 24, 26 (+ 25, 40 after decisions) | |
| **7. Global `~/.claude/CLAUDE.md`** | 10, 27, 43, 44 | |
| **8. Other skills** | 28, 29, 45, 46, 60 | |
| **9. Memory** | 30–36, 49–57, 62, 63 | Largest batch, lowest risk |
| **10. App source** | 23 | One comment in `~/git/ice-2` — needs its own PR in that repo |

## Owner decisions — not for an agent

- **#25** — §R4: narrow the prose to match the checker, or widen the checker? Widening needs a
  compliant path for a bordered box, which is why it was dropped.
- **#58** — the folder-backup deferral's stated trigger is met. Generalize `DragonBackup`, or
  restate the deferral with a new condition?
- **#23** — ice-2's source comment: correct the citation, or declare the exception it claims?
