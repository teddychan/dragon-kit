name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Build & test DragonKit
        run: swift test
      - name: Build Example app
        run: cd Example && swift build
